[build]
  command = "echo 'Static site, no build needed'"
  publish = "."

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/oauth"
  to = "/.netlify/functions/oauth"
  status = 200
